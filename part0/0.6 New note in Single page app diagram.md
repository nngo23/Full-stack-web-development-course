```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a new note and presses "Save"<br>Form is handled by JavaScript, not a normal POST form

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of browser: Request body contains JSON { content: "single page app does not reload the whole page", date: "2025-09-25" }<br>and header: (Content-Type: application/json)
    server-->>server: Server adds note to notes array
    server-->>browser: 201 Created with JSON of the new note
    deactivate server

    Note right of browser: The server does not redirect, the page is not reloaded<br>JS spa.js updates the DOM to display the new note


